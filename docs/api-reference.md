# Dog vs Cat CNN Library

**Base URL:** ``  
**Authentication:** 

## Overview
This repository provides a Python monolithic machine‑learning pipeline for classifying cat and dog images using a convolutional neural network. It includes modules for data preprocessing, model definition, training, evaluation, and an exploratory Jupyter notebook. The code is intended to be used as a library or script, not as a web service.

## Endpoints
No endpoints mapped.

